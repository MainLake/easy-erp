[pytest]
DJANGO_SETTINGS_MODULE = config.settings
pythonpath = backend
python_files = tests.py test_*.py *_tests.py
testpaths = backend

addopts =
    --tb=short
    --strict-markers
    -p no:warnings
